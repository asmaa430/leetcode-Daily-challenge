class Solution {
public:
    int minimumOperations(vector<int>& nums) {
        int n = (int)nums.size(), res = 0;
        for (int i = 0; i < n; i++) {
            res += ((nums[i] % 3) != 0);
        }
        return res;
    }
};